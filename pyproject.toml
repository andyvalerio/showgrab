[project]
name = "showgrab"
version = "0.1.0"
description = "Watch a showRSS feed and download new TV episodes through qBittorrent at a preferred quality, converging (up or down) as releases appear."
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "httpx>=0.27",
    "sqlalchemy>=2.0",
    "apscheduler>=3.10,<4",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
# core/ stays stdlib-only by convention (no import of httpx etc. outside
# adapters/ and service/), so the decision engine remains trivially testable
# and portable. The web UI (phase 4) adds Jinja2/HTMX on top of fastapi.
dev = [
    "pytest>=8",
]

[project.scripts]
showgrab = "showgrab.cli:main"
showgrab-serve = "showgrab.service.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Templates/static assets aren't .py files, so setuptools' default discovery
# skips them — found by actually running a built package (`pip install .`
# then executing it), not by any test, since every test/dev run imports from
# src/ directly and never exercises the packaged artifact.
showgrab = ["service/web/templates/**/*.html", "service/web/static/*"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
